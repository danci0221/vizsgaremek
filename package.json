{
  "name": "sporthub",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev:full": "npm run docker:db:up && node dev-full.mjs",
    "dev:full:app": "node dev-full.mjs",
    "docker:db:up": "docker compose -f docker-compose.yml up -d db phpmyadmin",
    "docker:db:down": "docker compose -f docker-compose.yml down",
    "docker:db:reset": "docker compose -f docker-compose.yml down -v",
    "docker:db:logs": "docker compose -f docker-compose.yml logs -f db",
    "test": "cd backend && npm test",
    "test:frontend:selenium": "cd Frontend && npm run test:selenium"
  },
  "dependencies": {
    "@docusaurus/core": "^3.10.0",
    "@docusaurus/module-type-aliases": "^3.10.0",
    "@docusaurus/preset-classic": "^3.10.0",
    "@docusaurus/types": "^3.10.0"
  }
}
